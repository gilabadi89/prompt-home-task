{
  "name": "extension",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/content.ts src/background.ts --bundle --outdir=dist/",
    "watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "esbuild": "0.25.0",
    "typescript": "^5.0.0"
  }
}